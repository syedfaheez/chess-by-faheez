version: '3.9'
services:
  server:
    build: ./server
    ports: ["8080:8080"]
  client:
    build: ./client
    ports: ["5173:80"]
    depends_on: [server]
    environment:
      - VITE_SERVER_URL=http://localhost:8080
